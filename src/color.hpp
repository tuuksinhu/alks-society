#pragma once
#include <SFML/Graphics.hpp>

namespace Colors {
inline const sf::Color Background = sf::Color(30, 30, 30);
inline const sf::Color playerColor = sf::Color::Green;

inline const sf::Color mainTextColor = sf::Color::Red;
}  // namespace Colors
